apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: bluegreen
spec:
  selector:
    app: frontend
    version: blue   # 👈 start by routing to blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80